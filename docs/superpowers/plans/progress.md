# SDD ledger — plan: docs/superpowers/plans/2026-08-17-agentic-incident-response-demo.md

## Setup

- Branch strategy: working directly on `main` (user's explicit choice — Task 1 establishes `main` itself; no existing history to protect since the GitHub repo doesn't exist yet).
- Ruling: work directly on `main`, no worktree isolation — Ruling recorded per human partner's explicit choice, not a controller decision. Cost if wrong: low (recoverable via git reflog / repo deletion before any real audience sees it).

## Pre-flight conflict scan

| Pair / Task | Produces | Consumes | Finding |
|---|---|---|---|
| Task 2 → Task 3 | `Record`, `IRecordStore`, `RecordStore(IReadOnlyList<Record>)`, `RecordStore.FromFixtureFile(string): RecordStore` | Task 3's `Program.cs` calls `RecordStore.FromFixtureFile(fixturePath)` and `store.GetRecordsForWindow(fromUtc, toUtc)` | Match, clean |
| Task 2 → Task 9 | `RecordStore.cs` contains literal line `r.TimestampUtc >= fromUtc && r.TimestampUtc <= toUtc` | Task 9's `seed-bug.js` `OLD_LINE` constant is that exact string | Match, clean |
| Task 2 → Task 5 | `app/ReconciliationJob.sln` | Task 5's `ci.yml` runs `dotnet test app/ReconciliationJob.sln` | Match, clean |
| Task 3 → Task 6 | `Program.cs` CLI contract (`fixturePath fromUtcIso toUtcIso` → `records_processed: N` on stdout), `fixtures/boundary-batch.json` | Task 6's `nightly-job.yml` invokes exactly that CLI shape against that fixture | Match, clean |
| Task 4 → Task 7 | `incident-log/incidents.json` shape `{date, symptom, symptomKeywords, rootCause, resolution}`; ADR files with first-line `keywords: [...]` | Task 7's `pretriage.js` `findPastIncident`/`findAdrMatch` read exactly those shapes | Match, clean |
| Task 6 → Task 7 | `simulate-alert.js` alert object `{metric, observed, expectedRange, timestampUtc, source}` | Task 7's `pretriage.js` `renderIssueBody` reads exactly those fields from `ALERT_PAYLOAD` | Match, clean |
| Task 7 → Task 8 | Issue labeled `incident:triage-ready` | Task 8's `incident-agent.yml` `label_trigger: "incident:triage-ready"` | Match, clean (identical string) |
| Task 5 → Task 9 | `ci.yml` job id `test`, produces check-runs on any PR | Task 9's `seed-bug.js` `waitForChecks` polls the check-runs API generically (no hardcoded job name) | Match, clean — ordering also correct (Task 9 runs after Task 5) |
| Tasks 6/7/8/9 → Task 10 | Exact filenames `nightly-job.yml`, `pretriage.yml`, `incident-agent.yml`, `seed.yml`, `ci.yml`, `simulate-alert.js`, `pretriage.js`, `seed-bug.js` | Task 10's `README.md` references all of these by name | Match, clean |
| Task 6 self-check | Task 6 Step 4 explicitly states full verification is deferred until Task 7 exists | N/A | Not a defect — the plan itself flags this; carry this note to Task 6's reviewer so partial verification isn't flagged as a spec gap |
| Task 8 write-scope claim | Spec/plan describe `src/**`, `docs/adr/**`, `incident-log/PIR-*.md` as an "allowlist" | Enforcement is prompt-only (`incident-responder.md` rule 7) — `claude_args`/`allowedTools` restricts *tools* (Read/Grep/Glob/Edit), not *paths* within Edit | Not a contradiction — spec never claims hard technical path enforcement, only an explicit, named scope vs. open-ended. Noted, not ruled on; carry as context to Task 8's reviewer so it isn't mistaken for a missed requirement. |

Scan result: clean. No rulings required before execution begins.

## Task log

Task 1: complete (controller-executed, not subagent-dispatched — Task 1 has no code interfaces and includes a credential-handling step that must stay with the human; ruling: controller runs the non-secret gh/git commands directly, human runs `gh secret set` themselves). Repo created and pushed: https://github.com/JurreBrandsen1709/agentic-incident-response-demo. Both secrets (`ANTHROPIC_API_KEY`, `DEMO_PAT`) confirmed present via `gh secret list`.

Task 2: review found 2 Critical — (1) target framework `net10.0` instead of mandated `net8.0` in both csproj files; (2) ~145 files of committed build output (bin/obj, no .gitignore) bloating the repo. ⚠️ "push actually ran" item resolved by controller directly: confirmed `origin/main` == local HEAD (952b0f3) — not a gap. Minor: stray `.slnx` file, missing EOF newline, FromFixtureFile untested (acceptable, deferred to Task 3 per brief) — logged as deferred minors, not entering the fix loop. Fix round 1 verified complete on resume (commit `0a04c31`): both csproj files confirmed `net8.0`, `git ls-files` confirms zero tracked `bin/`/`obj/` paths, `.gitignore` present with `**/bin/`/`**/obj/`. Task 2: **complete**.

## Environment constraint (applies to all remaining tasks)

- `dotnet` CLI is **not available** in this environment (confirmed: `Get-Command dotnet` fails, `CommandNotFoundException`). Human partner confirmed and asked to skip local test execution.
- Ruling: subagents for .NET tasks (Task 3 onward touching `app/`) write code and tests per TDD structure (test first, then implementation) but cannot execute `dotnet test`/`dotnet run` locally. They must say so explicitly rather than fabricate command output, and rely on careful static/manual verification (re-reading the code, checking types line-by-line) instead. Real verification happens later via `ci.yml` (Task 5) on GitHub Actions, which does have `dotnet` via `actions/setup-dotnet@v4`. Spec/quality reviewers for these tasks must not penalize "did not run dotnet test" as a defect, but must penalize any claim of having run it.

Task 3: implementer added `Program.cs` (CLI entrypoint) + `fixtures/normal-batch.json` + `fixtures/boundary-batch.json` (commit b7683a6). Spec review: ✅ compliant, byte-for-byte match to brief. Code quality review round 1: Changes Requested (Important, non-blocking-severity) — unhandled FormatException on malformed dates, unhandled exception on missing fixture, no guard against reversed fromUtc/toUtc silently producing misleading `records_processed: 0`. Fixed in commit `2c34d83` (try/catch + exit code scheme 0/1/2 + reversed-window guard). Re-review: ✅ Approved. Minor non-blocking follow-up noted (JsonException/InvalidOperationException not covered by the catch filter for malformed fixture JSON content) — logged as a deferred minor, not entering another fix loop (demo repo, low-value edge case, avoids over-engineering). Task 3: **complete**.

Task 4: implementer added `docs/adr/ADR-0001-utc-inclusive-ranges.md`, `incident-log/incidents.json`, `incident-log/PIR-template.md` (commit 6961e2a), verified JSON parses (node -e, length 1). Spec review: ✅ compliant, byte-identical to brief, committed and pushed. Code quality review: ✅ Approved. Forward-looking note (not a Task 4 defect, carry to Task 7's reviewer): the ADR keyword `records_processed` is stored as one token, but Task 7's `keywordsFromText()` tokenizer splits on non-alphanumerics including `_`, so that specific keyword will never round-trip through `overlapCount` against alert/diff text — other keywords (`boundary`, `inclusive`, etc.) still allow ADR matching to succeed, so not blocking, but Task 7's implementer/reviewer should be aware. Task 4: **complete**.

Task 5: implementer added `.github/workflows/ci.yml` (commit 7b4a3ba), carefully staged only that one file (untracked `.github/skills/` local tooling content confirmed NOT swept in). Spec review: ✅ compliant, byte-identical to brief, committed and pushed. Code quality review: ✅ Approved (only optional hardening suggestions — `timeout-minutes`, SHA-pinning, caching, concurrency groups — none required by the brief, not applied, avoids over-engineering a demo repo). Step 3 (manual throwaway-PR verification on the real GitHub repo) deferred to the human/controller per plan's own instructions — not yet executed. Task 5: **complete** (workflow authoring); manual GitHub verification still outstanding, to be done opportunistically alongside Task 11's end-to-end pass or sooner if the human wants to check it now.

Task 6: implementer added `scripts/simulate-alert.js` + `.github/workflows/nightly-job.yml` (commit eb33d97, byte-identical to brief; `node --check` passed). Spec review: ✅ compliant. Code quality review round 1: Changes Requested — 1 Critical (`permissions: contents: read` insufficient for the `POST /repos/{owner}/{repo}/dispatches` call `simulate-alert.js` makes; GitHub requires Contents:write for that endpoint, confirmed against GitHub's own REST API docs — a real anomaly would have failed to fire the alert with a 403/404), 1 Important (empty `COUNT` extraction silently coerced to `0` by the `if:` numeric comparison instead of failing loudly). Fixed in commit `44f9bc2` (`contents: write` + explicit empty-COUNT guard with `::error::` + `exit 1` before the `$GITHUB_OUTPUT` write). Re-review: ✅ Approved (non-blocking notes only — no `set -e` in the run step, comment-worthy string/number coercion in the `if:` — not applied, avoids over-engineering). Task 6: **complete**. Step 4 (live verification via `gh workflow run`) deferred — depends on Task 7 (`pretriage.yml`) not yet built, per the plan's own note.

Task 7: implementer added `scripts/pretriage.js` + `scripts/pretriage.test.js` + `.github/workflows/pretriage.yml` (commit 7779d2d), real TDD executed (RED: `Cannot find module './pretriage'` → GREEN: 5/5 passing, actual `node --test` output captured both times). Spec review: ✅ compliant, byte-identical/functionally-identical to brief. Code quality review round 1: Changes Requested — 2 Critical security findings (this script assembles evidence an LLM agent later reads and acts on, so unsanitized fields are a real prompt-injection surface): (1) issue title interpolated raw `alert.metric` bypassing `sanitizeForIssueBody`, (2) sanitizer didn't neutralize triple-backticks so evidence could break out of the ` ```json ` fence early into freeform Markdown. Fixed in commit `ff3dd21` (title now sanitized; sanitizer gained `.replace(/\`\`\`/g, "'''")`). Re-review: ✅ Approved, re-ran tests for real (5/5, no regression), verified both fixes with adversarial inputs. Non-blocking notes carried forward (not fixed, judged acceptable): `alert.observed`/`expectedRange` unsanitized in title (numeric/fixed-shape, not free text); `~~~` tilde-fence variant not neutralized; no regression test added for the two fixes. Design-level note (not a defect, flagged for the human): `sanitizeForIssueBody`'s phrase-matching is narrow (only the one literal "ignore previous instructions" phrasing) — broader prompt-injection coverage would be a spec change, not an implementation fix, deferred. Task 7: **complete**.

Task 8: implementer added `.github/agents/incident-responder.md` + `.github/workflows/incident-agent.yml` (commit 25b01bb, byte-identical to brief). Combined spec+quality review: ✅ Pass / ✅ Approved — permissions minimal and correct (`contents: write`, `pull-requests: write`, `issues: read`), heredoc `GITHUB_OUTPUT` pattern correct, `label_trigger` string matches Task 7's label creation exactly, prompt-only write-scope enforcement confirmed consistent with the plan's known/accepted design (not re-flagged). Task 8: **complete**.

Task 9: implementer added `scripts/seed-bug.js` + `.github/workflows/seed.yml` (commit 3e91666, byte-identical to brief; `node --check` passed). Combined spec+quality review: ✅ Pass / ✅ Approved. Confirmed `OLD_LINE` literal (`r.TimestampUtc >= fromUtc && r.TimestampUtc <= toUtc`) still present verbatim in `RecordStore.cs` — seed script will not throw on this repo. Only Minor notes, not requiring fixes: `pull-requests: write` permission technically unused (PR opened via `DEMO_PAT`, not the job's default token — correct design, just makes that grant redundant); non-atomic re-run ordering (branch created before the OLD_LINE guard fires) gives a less-friendly error on a second run, acceptable per the script's own "run once per instance" doc comment. Task 9: **complete**.

## BLOCKER discovered during Task 10: `gh` CLI authenticated as the wrong GitHub account

`gh auth status` shows account `jurre-brandsen_bngbank`, which has only `pull` access (admin=false, push=false) to `JurreBrandsen1709/agentic-incident-response-demo`. `git push` has been succeeding throughout Tasks 2-10 anyway because git uses a SEPARATE credential via Windows Git Credential Manager (`credential.helper=manager`), evidently authenticated as `JurreBrandsen1709` (which does have push rights) — `gh api` calls use the `gh auth` keyring credential instead, which is the wrong, read-only account. Confirmed via `gh api repos/JurreBrandsen1709/agentic-incident-response-demo/branches/main/protection` PUT → `404 Not Found` (not 403 — consistent with insufficient access, not a missing/broken endpoint), and `gh api repos/.../.` `.permissions` → `{"admin": false, "push": false, "pull": true}`. Noted in `/memories/repo/gh-auth-note.md` for future sessions.

**Impact:**
- Task 10 Step 1 (branch protection PUT) and Step 2 (`is_template=true` PATCH) — BLOCKED, need admin access.
- Task 10 Step 3 (README.md) — NOT blocked, no GitHub API admin call involved. Completed (see below).
- Task 11 (end-to-end verification: `gh workflow run`, `gh pr merge`, `gh issue list` against the live repo) — will ALSO be blocked by this same read-only account for any write-ish `gh` command, once we get there.

**Resolution needed from the human:** either `gh auth login`/`gh auth switch` to the `JurreBrandsen1709` account (or a token with admin rights on this repo), or the human runs the blocked admin-only commands themselves. Flagging to the human now rather than proceeding further with Task 10/11's live-repo steps under the wrong account.

**Resolved:** human ran `gh auth login` as `JurreBrandsen1709` (now the active `gh` account, confirmed `admin: true` via `gh api repos/.../.` `.permissions`). Controller retried the two blocked infra steps directly (Task 1 precedent — infra-only, no code interfaces):
- Branch protection PUT: first attempt returned `400 Problems parsing JSON` — root cause was a UTF-8 BOM in the temp JSON file written by PowerShell's `Set-Content -Encoding utf8`. Rewrote the file via `[System.IO.File]::WriteAllText(..., [System.Text.UTF8Encoding]::new($false))` (no BOM) — retry succeeded, confirmed via response body (`enforce_admins.enabled: true`, `required_pull_request_reviews.required_approving_review_count: 0`).
- `is_template=true` PATCH: succeeded on first attempt, confirmed via `.jq '.is_template'` → `true`.

Task 10: **complete** (README + both infra steps done).

Task 10 (partial): implementer added `README.md` (commit 03107a6, 73 lines). Combined spec+quality review: ✅ Pass / ✅ Approved — every filename/path mentioned cross-checked against the actual repo state and confirmed present; narrative claims (boundary bug behavior, label name, action name, "everything real except the simulated alert") verified accurate against the actual code, not just prose. Minor non-blocking nit: `scripts/` bullet list doesn't mention `pretriage.test.js` (test file, not operational script — not misleading, not fixed). Step 1 (branch protection) and Step 2 (mark as template) are BLOCKED on the gh-auth issue above — not yet executed. Task 10: **README complete, infrastructure steps blocked pending human action**.
